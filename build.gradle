allprojects {
    group = 'org.openchs'
    version = '0.0.1-SNAPSHOT'
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'idea'

    repositories {
        mavenLocal()
        maven { url "https://repo.maven.apache.org/maven2" }
    }
}
