{
  "query": "query groups($filter: GroupFilter, $opts: Opts) { groups(filter: $filter, opts:$opts) {   id   label   isRestricted   contactsCount   usersCount   description } }",
  "variables": {
    "opts": {
      "order": "ASC",
      "limit": "${limit}",
      "offset": "${offset}"
    }
  }
}
